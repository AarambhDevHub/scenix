# Asset Manager

Demonstrates package caching, memory accounting, and stale dependency invalidation.

`examples/asset_manager.rs`

Required features:

`loader`

```sh
cargo run -p scenix --example asset_manager --features loader
```
