# Animation Import

Shows the v1.3 imported animation clip sidecar types used by asset packages.

`examples/animation_import.rs`

Required features:

`loader`

```sh
cargo run -p scenix --example animation_import --features loader
```
