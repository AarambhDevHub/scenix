# Export Scene

Loads a generated STL fixture and exports scene JSON plus ASCII STL output.

`examples/export_scene.rs`

Required features:

`loader`

```sh
cargo run -p scenix --example export_scene --features loader
```
