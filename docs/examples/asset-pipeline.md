# Asset Pipeline

Loads a generated glTF file into an `AssetPackage` and uploads it through `RendererAssetExt`.

`examples/asset_pipeline.rs`

Required features:

`loader`, `renderer`

```sh
cargo run -p scenix --example asset_pipeline --features "loader renderer"
```
