# Compressed Assets

Prints the v1.3 asset format support matrix and compressed asset diagnostics.

`examples/compressed_assets.rs`

Required features:

`loader`

```sh
cargo run -p scenix --example compressed_assets --features loader
```
